<%= form_with model:[book,book_comment], remote: true do |f| %>
  <div id="comments-error">
    <%= render 'books/error_messages', model: book_comment %>
  </div>
  <%= f.text_area :comment, size:"90x5" %><br>
  <%= f.submit '送信'%>
<% end %>